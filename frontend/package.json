{
  "name": "icoder-plus-frontend-static",
  "version": "2.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && echo 'Static build complete'",
    "start": "echo 'Static site ready'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
